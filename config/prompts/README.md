# Prompts Runtime

Questa cartella conterra' i prompt runtime usati dal codice.

I file in `dev_support/prompts/` restano il riferimento di progettazione.
Quando il sistema verra' collegato ai provider LLM, i prompt caricati a runtime vivranno qui.
